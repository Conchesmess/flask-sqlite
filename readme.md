Make sure you are in the right python environment bt going to the "View" menu, then choose "Command Palette" then search for "Python: Select Environment". Pick the one called (Base) that is 3.13 or greater. Then delete the terminal window by clicking the little trash can next reen.  It will to the terminal list in the bottom right of the screen. It will be a list of things like "Python" "Bash" and/or "zsh". Then open a new terminal using the Terinal menu.

install the needed python modules by typing this command in the terminal: 
pip install -r requirements.txt

Get the client_id and client_secret values from google classroom and put them in secretBLANK.py file that 
is in the routes folder that is in the app folder

rename the secretBLANK.py file to secret.py

run the main.py file

open your browser to https://127.0.0.1:8080



SQL tutorial: https://dev.to/freddiemazzilli/flask-sqlalchemy-relationships-exploring-relationship-associations-igo